# Python CRUD Game

This simple Python application lets you perform CRUD operations on a list of players.

## Setup

1. Create a virtual environment:
   ```powershell
   python -m venv .venv
   .venv\Scripts\activate
   ```
2. Run `python main.py` to start the menu-driven interface.
